import 'package:flutter/material.dart';

class AppIcons {
  static const Icon mail = Icon(Icons.mail);
  static const Icon lock = Icon(Icons.lock);
  static const Icon menue = Icon(Icons.menu);
  static const Icon scan = Icon(Icons.scanner);
  static const IconData error = Icons.error;
  static const IconData edit = Icons.edit;

  static const IconData warning = Icons.warning;
  static const IconData circle = Icons.check_circle;
}
